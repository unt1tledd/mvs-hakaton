# 📖 Руководство пользователя VK Content Analytics

## 🚀 Быстрый старт

### Шаг 1: Подготовка данных

Перед использованием приложения вам понадобятся:

1. **API токен MWS Tables**
2. **URL таблицы MWS**
3. **API токен VK**
4. **ID группы VK**

---

## 📋 Где взять данные для каждого поля

### 1️⃣ API токен MWS (Шаг 1)

**Где взять:**
1. Зайдите на https://tables.mws.ru
2. Войдите в свой аккаунт (или создайте новый)
3. Перейдите в настройки профиля
4. Найдите раздел "API" или "Токены доступа"
5. Создайте новый токен или скопируйте существующий

**Формат:** `mws_xxxxxxxxxxxxxxxxxxxxxxxxx`

**Пример:** `mws_abc123def456ghi789jkl012mno345`

**Что это:**
API токен для доступа к MWS Tables - облачному хранилищу данных, куда будут сохраняться посты из VK.

---

### 2️⃣ Имя таблицы (Шаг 2)

**Как заполнить:**
Просто введите понятное название для вашей таблицы.

**Примеры:**
- `Посты МТС`
- `Контент Telegram канала`
- `Аналитика сообщества`
- `Статистика постов 2025`

**Что это:**
Человекочитаемое название таблицы для удобства навигации.

---

### 3️⃣ URL таблицы MWS (Шаг 2)

**Где взять:**
1. Зайдите на https://tables.mws.ru
2. Создайте новую таблицу:
   - Нажмите "Создать таблицу" или "New Datasheet"
   - Дайте таблице название
   - Выберите структуру (можно использовать шаблон для постов)
3. Откройте созданную таблицу
4. Скопируйте URL из адресной строки браузера

**Формат:** `https://tables.mws.ru/datasheets/{DATASHEET_ID}/views/{VIEW_ID}`

**Пример:** `https://tables.mws.ru/datasheets/dst_abc123xyz/views/viw_def456uvw`

**Где найти:**
- В адресной строке браузера когда открыта таблица
- `DATASHEET_ID` - это ID самой таблицы (после `/datasheets/`)
- `VIEW_ID` - это ID представления таблицы (после `/views/`)

**Что это:**
Прямая ссылка на вашу таблицу в MWS Tables, куда будут сохраняться данные.

---

### 4️⃣ API токен VK (Шаг 2)

**Где взять:**

#### Вариант 1: Сервисный ключ доступа (рекомендуется)
1. Зайдите на https://vk.com/dev
2. Перейдите в "Мои приложения"
3. Создайте Standalone-приложение:
   - Нажмите "Создать приложение"
   - Название: например "VK Analytics"
   - Платформа: "Standalone приложение"
4. В настройках приложения найдите "Сервисный ключ доступа"
5. Скопируйте токен

#### Вариант 2: Получить токен через OAuth
1. Используйте VK API Explorer: https://vk.com/dev/auth_sites
2. Выберите необходимые права доступа (wall, groups)
3. Получите токен доступа

**Формат:** Длинная строка символов

**Пример:** `vk1.a.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

**Права доступа:** Убедитесь что токен имеет права на чтение постов со стены (scope: `wall`, `groups`)

**Что это:**
Ключ для доступа к VK API, чтобы читать посты из групп.

---

### 5️⃣ ID группы VK (Шаг 2)

**Где взять:**
1. Откройте группу VK в браузере
2. Посмотрите на URL в адресной строке

**Формат URL:** `https://vk.com/club12345678` или `https://vk.com/public12345678`

**Извлечение ID:**
- Если URL: `vk.com/club12345678` → ID группы: `12345678`
- Если URL: `vk.com/public12345678` → ID группы: `12345678`
- Если URL: `vk.com/mts` (короткое имя):
  1. Откройте группу
  2. Кликните правой кнопкой на аватар группы
  3. "Скопировать ссылку на изображение"
  4. В ссылке будет: `pp.userapi.com/.../-123456789/...`
  5. ID группы: `123456789` (без минуса)

**Альтернативный способ:**
1. Откройте группу
2. Нажмите F12 (Developer Tools)
3. В консоли выполните: `VK.id` или `vk.widget.id`

**Формат:** Только цифры (без минуса и без "club"/"public")

**Примеры:**
- `12345678`
- `87654321`

**Что это:**
Уникальный идентификатор группы VK, из которой будут собираться посты.

---

### 6️⃣ Количество постов (Шаг 2)

**Как заполнить:**
Укажите сколько последних постов нужно получить из группы.

**Формат:** Число от 1 до 100

**Примеры:**
- `10` - последние 10 постов
- `50` - последние 50 постов
- `100` - последние 100 постов

**По умолчанию:** `10`

**Ограничения:**
- Минимум: 1 пост
- Максимум: 100 постов (ограничение VK API)

**Что это:**
Количество последних постов, которые будут получены из группы и сохранены в таблицу.

---

## 🎯 Пошаговая инструкция

### Шаг 1: Настройка API MWS

1. Откройте приложение: http://localhost:3000
2. Нажмите кнопку **"Начать"**
3. Введите **API токен MWS** (получен на https://tables.mws.ru)
4. (Опционально) Нажмите **"Скачать шаблон таблицы"** чтобы получить CSV шаблон
5. Нажмите **"Далее"**

---

### Шаг 2: Настройка таблицы и VK

1. **Имя таблицы:** Введите название (например: "Посты МТС")
2. **URL таблицы MWS:** Вставьте ссылку на вашу таблицу
3. **API токен VK:** Вставьте токен доступа к VK API
4. **ID группы VK:** Введите ID группы (только цифры)
5. **Количество постов:** Укажите сколько постов собрать (по умолчанию 10)
6. Нажмите **"Далее"**

---

### Шаг 3: Просмотр данных

После сохранения конфигурации вы увидите:

1. **Карточки со статистикой:**
   - Всего постов
   - Общее количество просмотров
   - Общее количество лайков
   - Общее количество комментариев

2. **Таблица топ-10 постов** по просмотрам

3. **Кнопки управления:**
   - **"Обновить данные"** - запустить парсинг VK и обновить таблицу
   - **"Экспорт CSV"** - скачать данные в CSV формате
   - **"Экспорт JSON"** - скачать данные в JSON формате

---

## 🔧 Частые вопросы

### ❓ Как создать таблицу в MWS Tables?

1. Зайдите на https://tables.mws.ru
2. Нажмите "Создать таблицу"
3. Выберите шаблон или создайте пустую
4. Скопируйте URL созданной таблицы

### ❓ Как получить права на чтение постов VK?

При создании токена VK убедитесь что выбраны права:
- `wall` - чтение постов со стены
- `groups` - работа с группами

### ❓ Почему не находится ID группы?

- Используйте только цифры (без "club", "public", "-")
- Для групп с коротким именем откройте группу и найдите ID в коде страницы

### ❓ Сколько постов можно получить за раз?

VK API позволяет получить максимум 100 постов за один запрос.

---

## 🛠️ Решение проблем

### Ошибка "401 Unauthorized"
- Проверьте правильность API токена MWS
- Убедитесь что токен активен

### Ошибка при парсинге VK
- Проверьте API токен VK
- Убедитесь что токен имеет нужные права
- Проверьте правильность ID группы

### Данные не отображаются
- Нажмите кнопку "Обновить данные"
- Проверьте что таблица MWS создана
- Проверьте URL таблицы

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Developer Tools (F12)
2. Убедитесь что все токены действительны
3. Перезапустите приложение

---

## ✨ Дополнительные возможности

### Экспорт данных
- **CSV** - для работы в Excel, Google Sheets
- **JSON** - для программной обработки

### Автообновление (планируется)
- Автоматический парсинг VK по расписанию
- Уведомления о новых постах

### Визуализация (планируется)
- Графики просмотров по времени
- Аналитика вовлеченности
- Сравнение постов

---

**Версия:** 1.0
**Дата:** 2025-11-30
